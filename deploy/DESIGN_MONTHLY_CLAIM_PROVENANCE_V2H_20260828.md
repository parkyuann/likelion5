# DESIGN — monthly claim provenance v2h candidate identity seal

Canonical authority is
`docs/고도화/설계도_월별주장_provenance_연결_v2h_20260828.md`, cumulative with v2b-v2g.
Transformed profiles are revalidated for membership table key and non-empty inventories.
Projection and assignment table identities must exactly match the fixed membership before coverage
or uniqueness; any mismatch yields `CANDIDATE_IDENTITY_MISMATCH` with no query plan.
